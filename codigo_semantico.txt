var
    x, y : integer;
    promedio : real;
    nombre : string;
    esValido : boolean;

begin
    x := 10;
    y := 20;
    
    
    promedio := (x + y) / 2;
    
    nombre := "Proyecto Semantico";
    esValido := true;

   

  
    z := 5; 

    
    x := 3.14;

    
    y := x + nombre;

    
    if x then
        begin
            x := x + 1;
        end;

end.
